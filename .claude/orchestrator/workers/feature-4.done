## Feature 4 Completed: SQLAlchemy Models for Domain, Task, Flashcard, Question

### Summary
Created SQLAlchemy models for the core PMP 2026 Study Application content hierarchy.

### Files Created
1. `backend/app/models/domain.py` - Domain model
2. `backend/app/models/task.py` - Task model
3. `backend/app/models/flashcard.py` - Flashcard model
4. `backend/app/models/question.py` - Question model

### Model Details

**Domain Model** (`domains` table):
- `id`: Primary key
- `name`: Unique domain name (String 100)
- `description`: Optional description (String 500)
- `weight`: Exam weight percentage (Float)
- `order`: Display order (Integer)
- Relationship: One-to-many with Task

**Task Model** (`tasks` table):
- `id`: Primary key
- `domain_id`: Foreign key to domains (CASCADE delete)
- `name`: Task name (String 200)
- `description`: Optional description (Text)
- `order`: Display order (Integer)
- Relationships: Many-to-one with Domain, One-to-many with Flashcard and Question

**Flashcard Model** (`flashcards` table):
- `id`: Primary key
- `task_id`: Foreign key to tasks (CASCADE delete)
- `front`: Card front/question (Text)
- `back`: Card back/answer (Text)
- `created_at`, `updated_at`: Timestamps with timezone
- Relationship: Many-to-one with Task

**Question Model** (`questions` table):
- `id`: Primary key
- `task_id`: Foreign key to tasks (CASCADE delete)
- `question_text`: Question content (Text)
- `option_a`, `option_b`, `option_c`, `option_d`: Answer options (Text)
- `correct_answer`: Correct option letter A-D (String 1)
- `explanation`: Answer explanation (Text)
- `difficulty`: Optional difficulty level (String 20)
- `created_at`, `updated_at`: Timestamps with timezone
- Relationship: Many-to-one with Task

### Relationships
- Domain → Task (1:N with cascade delete)
- Task → Domain (N:1 back_populates)
- Task → Flashcard (1:N with cascade delete)
- Task → Question (1:N with cascade delete)
- Flashcard → Task (N:1 back_populates)
- Question → Task (N:1 back_populates)

### Notes
- All models use SQLAlchemy 2.0 Mapped types
- TYPE_CHECKING imports prevent circular dependency issues
- Cascading deletes ensure referential integrity
- Models are exported via `backend/app/models/__init__.py`
